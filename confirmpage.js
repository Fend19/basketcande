  // Append elements to this declaration
  let confirmpageSection = (document.querySelector(
    ".confirmArticle"
  ).innerHTML = "hej");
  let confirmpage = document.querySelector(".mainConfirmPage");
  confirmpage.appendChild(confirmpageSection);
  console.log(confirmpageSection);